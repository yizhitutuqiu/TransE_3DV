model:
  ckpt_path: checkpoints/transe_v1/best.pt
  data_dir: data/preprocessed/final
  device: auto
  embedding_dim: 100
  p_norm: 1

infer:
  top_k: 10
  batch_size: 512
  filtered: true

eval:
  batch_size: 256

limits:
  max_k: 100
  max_batch_queries: 256
