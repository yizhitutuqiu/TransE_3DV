model:
  ckpt_path: checkpoints/transe_v3/best.pt
  data_dir: data/preprocessed/final
  device: auto
  embedding_dim: auto
  p_norm: auto

metadata:
  documents_path: data/preprocessed/text/documents.jsonl
  include_in_search: true

infer:
  top_k: 10
  batch_size: 512
  filtered: true
  mode: hybrid

eval:
  batch_size: 256

limits:
  max_k: 100
  max_batch_queries: 256
